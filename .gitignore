# NEXUS - Do not commit these files
.env
*.pyc
__pycache__/
.DS_Store
.vscode/
*.log
BIDS:RESOURCES/

# NEMT local trip log (PHI / Medicaid IDs)
nemt_billing_data.json
nemt_billing_data.json.tmp

# NEMT factoring invoices (HTML/PDF; may contain PHI)
GENERATED_VERTEX_INVOICES/

# MiLogin — optional local notes (passwords); never commit
MILOGIN_ACCOUNTS.local.md
